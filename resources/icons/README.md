# Application icons

- `devsnapshot.png` is the high-resolution source used inside the application.
- `devsnapshot.ico` contains Windows shell sizes used by the EXE and installer.

Keep the icon filenames stable because the source launcher, PyInstaller spec, and
Inno Setup definition reference them directly.
